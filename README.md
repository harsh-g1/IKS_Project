## Project Commands (in terminal):

``` npm install ```\
``` npm run dev ```

If above commands don't work, then use:\
``` Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force ```\

## Commands for running Mock Server:

``` npm install ws ```\
``` node mock_hr_server.cjs ```

 
